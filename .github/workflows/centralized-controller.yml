# =============================================================================
# CENTRALIZED TERRAFORM CONTROLLER - The Real Deal!
# =============================================================================
# This workflow runs in the CENTRALIZED repo
# It listens for events from dev repos and does ALL the work
# Uses centralized main.tf, scripts, policies - dev repos just trigger it
# =============================================================================

name: Centralized Controller

on:
  repository_dispatch:
    types: [terraform_pr, terraform_apply]

permissions:
  id-token: write
  contents: read
  actions: read

env:
  TERRAFORM_VERSION: '1.11.0'
  OPA_VERSION: '0.59.0'
  AWS_REGION: 'us-east-1'

jobs:
  terraform-controller:
    name: 🎯 Terraform Controller
    runs-on: ubuntu-latest
    steps:
      - name: 📋 Event Details
        run: |
          echo "🔔 Repository Dispatch Event Received"
          echo "Source Repo: ${{ github.event.client_payload.source_owner }}/${{ github.event.client_payload.source_repo }}"
          echo "PR Number: ${{ github.event.client_payload.pr_number }}"
          echo "Action: ${{ github.event.client_payload.action }}"
          if [[ "${{ github.event.client_payload.action }}" == "apply" ]]; then
            echo "🚀 APPLY MODE - Running terraform apply"
            echo "PR Head: ${{ github.event.client_payload.pr_head_ref }}"
            echo "PR SHA: ${{ github.event.client_payload.pr_head_sha }}"
            echo "Merged SHA: ${{ github.event.client_payload.merge_commit_sha }}"
          else
            echo "🔍 PLAN MODE - Running terraform plan and validation"
            echo "PR Head: ${{ github.event.client_payload.pr_head_ref }}"
            echo "PR SHA: ${{ github.event.client_payload.pr_head_sha }}"
          fi

      - name: Generate GitHub App Token
        id: app-token
        uses: tibdex/github-app-token@v2
        with:
          app_id: ${{ secrets.GT_APP_ID }}
          private_key: ${{ secrets.GT_APP_PRIVATE_KEY }}

      - name: Checkout Centralized Controller Repo
        uses: actions/checkout@v4
        with:
          repository: Terraform-centilazed-pipline/centerlized-pipline-
          path: controller
          token: ${{ steps.app-token.outputs.token }}

      - name: Checkout Source Dev Repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.client_payload.source_owner }}/${{ github.event.client_payload.source_repo }}
          ref: ${{ github.event.client_payload.action == 'apply' && 'main' || github.event.client_payload.pr_head_ref }}
          path: source-repo
          token: ${{ steps.app-token.outputs.token }}

      - name: Checkout OPA Policies
        uses: actions/checkout@v4
        with:
          repository: Terraform-centilazed-pipline/OPA-Poclies
          path: opa-policies
          token: ${{ steps.app-token.outputs.token }}

      # Note: tf-module checkout removed - Terraform fetches modules from GitHub automatically
      # Module sources now use: git::https://github.com/mmctech/tf-module.git//Module/<NAME>

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_TERRAFORM_ROLE_ARN}}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: terraform-controller

      - name: 🔧 Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: 🔧 Setup OPA
        run: |
          echo "📦 Installing OPA ${{ env.OPA_VERSION }}..."
          curl -L -o opa https://openpolicyagent.org/downloads/v${{ env.OPA_VERSION }}/opa_linux_amd64_static
          chmod +x opa
          sudo mv opa /usr/local/bin/
          opa version

      - name: Configure Git Authentication for Terraform Modules
        run: |
          echo "🔧 Configuring Git authentication for private Terraform modules..."
          git config --global url."https://oauth2:${{ steps.app-token.outputs.token }}@github.com".insteadOf "https://github.com"
          echo "✅ Git authentication configured for Terraform module downloads"

      # ===== Get changed files from PR (skip for apply mode) =====
      - name: 🔍 Get Changed Files
        if: github.event.client_payload.action != 'apply'
        id: changed-files
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const { data: files } = await github.rest.pulls.listFiles({
              owner: '${{ github.event.client_payload.source_owner }}',
              repo: '${{ github.event.client_payload.source_repo }}',
              pull_number: ${{ github.event.client_payload.pr_number }}
            });
            
            const tfvarsFiles = files
              .filter(f => f.filename.match(/\.(tfvars|yaml|yml|json)$/))
              .filter(f => f.filename.startsWith('Accounts/') || f.filename.startsWith('configs/'))
              .map(f => f.filename);
            
            console.log('Changed files:', tfvarsFiles);
            core.setOutput('files', tfvarsFiles.join(' '));
            core.setOutput('count', tfvarsFiles.length);

      # ===== STEP 1: DISCOVER DEPLOYMENTS =====
      - name: 🔍 Discover Deployments
        if: github.event.client_payload.action != 'apply' && steps.changed-files.outputs.count > 0
        id: discover
        working-directory: source-repo
        env:
          TERRAFORM_DIR: ../controller
        run: |
          echo "🔍 Running discovery for plan mode..."
          echo "Changed files: ${{ steps.changed-files.outputs.files }}"
          echo "Changed files count: ${{ steps.changed-files.outputs.count }}"
          python3 ../controller/scripts/terraform-deployment-orchestrator.py discover \
            --changed-files "${{ steps.changed-files.outputs.files }}" \
            --output-summary deployments.json \
            --debug
          
          total=$(jq -r '.total_deployments // 0' deployments.json)
          echo "total=$total" >> $GITHUB_OUTPUT
          
          if [ "$total" -gt 0 ]; then
            echo "✅ Found $total deployments"
            echo "has_deployments=true" >> $GITHUB_OUTPUT
            
            # Extract first tfvars file for pre-validation
            first_tfvars=$(jq -r '.deployments[0].tfvars_file // empty' deployments.json)
            echo "first_tfvars=$first_tfvars" >> $GITHUB_OUTPUT
          else
            echo "ℹ️ No deployments found"
            echo "has_deployments=false" >> $GITHUB_OUTPUT
          fi

      # ===== APPLY MODE: Discover from merged files =====
      - name: 🔍 Discover Deployments (Apply Mode)
        if: github.event.client_payload.action == 'apply'
        id: discover-apply
        working-directory: source-repo
        env:
          TERRAFORM_DIR: ../controller
        run: |
          echo "🔍 Running discovery for apply mode..."
          echo "Apply mode - using changed files: ${{ github.event.client_payload.changed_files || github.event.client_payload.files }}"
          # For apply mode, get changed files from the merge commit
          python3 ../controller/scripts/terraform-deployment-orchestrator.py discover \
            --changed-files "${{ github.event.client_payload.changed_files || github.event.client_payload.files }}" \
            --output-summary deployments.json \
            --debug
          
          total=$(jq -r '.total_deployments // 0' deployments.json)
          echo "total=$total" >> $GITHUB_OUTPUT
          
          if [ "$total" -gt 0 ]; then
            echo "✅ Found $total deployments for apply"
            echo "has_deployments=true" >> $GITHUB_OUTPUT
          else
            echo "ℹ️ No deployments found for apply"
            echo "has_deployments=false" >> $GITHUB_OUTPUT
          fi

      # ===== STEP 2: PRE-DEPLOYMENT VALIDATION (BEFORE terraform plan) =====
      - name: 🔍 Pre-Deployment Validation
        if: steps.discover.outputs.has_deployments == 'true'
        id: pre_validation
        working-directory: source-repo
        continue-on-error: true
        run: |
          echo "🔍 Running pre-deployment validation..."
          echo "📄 Validating: ${{ steps.discover.outputs.first_tfvars }}"
          echo "👤 PR Author: ${{ github.event.client_payload.pr_author }}"
          
          # Run pre-deployment validator
          set +e
          python3 ../controller/scripts/pre-deployment-validator.py \
            "${{ steps.discover.outputs.first_tfvars }}" \
            "${{ github.event.client_payload.pr_author }}"
          
          validation_exit=$?
          set -e
          
          if [ -f pre-validation-comment.md ]; then
            echo "validation_comment_exists=true" >> $GITHUB_OUTPUT
          fi
          
          if [ $validation_exit -eq 0 ]; then
            echo "✅ Pre-deployment validation passed"
            echo "validation_passed=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Pre-deployment validation failed"
            echo "validation_passed=false" >> $GITHUB_OUTPUT
          fi
          
          exit $validation_exit

      - name: 💬 Post Pre-Validation Comment
        if: steps.pre_validation.outputs.validation_comment_exists == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const fs = require('fs');
            const comment = fs.readFileSync('source-repo/pre-validation-comment.md', 'utf8');
            
            await github.rest.issues.createComment({
              owner: '${{ github.event.client_payload.source_owner }}',
              repo: '${{ github.event.client_payload.source_repo }}',
              issue_number: ${{ github.event.client_payload.pr_number }},
              body: comment
            });

      - name: 🛑 Stop if Pre-Validation Failed
        if: steps.pre_validation.outputs.validation_passed == 'false'
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            // Close the PR
            await github.rest.pulls.update({
              owner: '${{ github.event.client_payload.source_owner }}',
              repo: '${{ github.event.client_payload.source_repo }}',
              pull_number: ${{ github.event.client_payload.pr_number }},
              state: 'closed'
            });
            
            core.setFailed('❌ Pre-deployment validation failed. PR closed.');

      # ===== STEP 2.5: SECURITY SCAN WITH CHECKOV =====
      - name: 🛡️ Security Scan with Checkov
        if: (steps.discover.outputs.has_deployments == 'true') || (steps.discover-apply.outputs.has_deployments == 'true')
        id: security
        working-directory: source-repo
        continue-on-error: true
        run: |
          echo "🛡️ Running Checkov security scan..."
          echo "📁 Working directory: $(pwd)"
          echo "📂 Directory contents:"
          ls -la
          
          # Setup Python environment
          echo "🐍 Setting up Python environment..."
          python3 --version
          pip3 --version
          
          # Check if we have any Terraform-related files to scan
          echo "🔍 Looking for files to scan..."
          tf_files=$(find . -name "*.tf" -o -name "*.tfvars" -o -name "*.json" | grep -E "\.(tf|tfvars|json)$" | head -10)
          
          if [ -z "$tf_files" ]; then
            echo "⚠️ No Terraform files found to scan"
            echo "📂 Available files:"
            find . -name "*.tf*" -o -name "*.json" | head -10 || echo "No .tf, .tfvars, or .json files found"
            echo "scan_status=skipped" >> $GITHUB_OUTPUT
            echo "scan_message=⚠️ No Terraform files found to scan" >> $GITHUB_OUTPUT
            echo "failed_checks=0" >> $GITHUB_OUTPUT
            echo "passed_checks=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "✅ Found files to scan:"
          echo "$tf_files"
          
          # Install Checkov with proper error handling
          echo "📦 Installing Checkov..."
          pip3 install --upgrade pip || echo "Failed to upgrade pip, continuing..."
          pip3 install checkov || {
            echo "❌ Failed to install Checkov with pip3, trying pip..."
            pip install checkov || {
              echo "❌ Failed to install Checkov completely"
              echo "scan_status=skipped" >> $GITHUB_OUTPUT
              echo "scan_message=⚠️ Failed to install Checkov" >> $GITHUB_OUTPUT
              echo "failed_checks=0" >> $GITHUB_OUTPUT
              echo "passed_checks=0" >> $GITHUB_OUTPUT
              exit 0
            }
          }
          
          # Verify Checkov installation
          echo "🔧 Verifying Checkov installation..."
          checkov --version || {
            echo "❌ Checkov installation verification failed"
            echo "scan_status=skipped" >> $GITHUB_OUTPUT
            echo "scan_message=⚠️ Checkov installation verification failed" >> $GITHUB_OUTPUT
            echo "failed_checks=0" >> $GITHUB_OUTPUT
            echo "passed_checks=0" >> $GITHUB_OUTPUT
            exit 0
          }
          
          # Create results directory
          mkdir -p checkov-results
          
          # Use discovered deployments from earlier step  
          if [ ! -f "deployments.json" ]; then
            echo "❌ No deployments.json found - skipping security scan"
            echo "📁 Debug - checking for deployments.json in current directory:"
            ls -la . | grep deployments || echo "No deployments.json file found"
            echo "📁 Current working directory: $(pwd)"
            echo "📁 Available files:"
            ls -la . | head -10
            echo "scan_status=skipped" >> $GITHUB_OUTPUT
            echo "scan_message=ℹ️ No deployments discovered" >> $GITHUB_OUTPUT
            echo "failed_checks=0" >> $GITHUB_OUTPUT
            echo "passed_checks=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "✅ Found deployments.json, extracting tfvars files..."
          cat deployments.json | jq '.'
          
          # Extract all tfvars files from discovered deployments
          tfvars_files=$(jq -r '.deployments[]?.tfvars_file // .deployments[]?.file' deployments.json | tr '\n' ' ')
          
          if [ -n "$tfvars_files" ] && [ "$tfvars_files" != " " ]; then
            # Convert relative paths to full paths (we're already in source-repo directory)
            full_paths=""
            for file in $tfvars_files; do
              if [[ "$file" != "null" ]] && [ -n "$file" ]; then
                full_paths="$full_paths ./$file"
              fi
            done
            
            if [ -n "$full_paths" ]; then
              echo "🎯 Found .tfvars files for scan: $full_paths"
              echo "📁 Scanning controller .tf files with specific .tfvars values..."
              
              # Checkov correct approach: scan .tf files with --var-file for specific .tfvars
              echo "🔍 Running Checkov on controller configuration with variable files..."
              
              # Build var-file arguments for each tfvars file
              var_file_args=""
              for file in $full_paths; do
                if [[ "$file" != "null" ]] && [ -n "$file" ] && [ -f "$file" ]; then
                  # Convert relative path to absolute path
                  abs_path="$(pwd)/$file"
                  var_file_args="$var_file_args --var-file=$abs_path"
                  echo "📋 Adding var file: $abs_path"
                fi
              done
              
              if [ -n "$var_file_args" ]; then
                echo "🔍 Running: checkov -d ../controller $var_file_args"
                checkov --framework terraform \
                  --directory ../controller \
                  $var_file_args \
                  --output json \
                  --output-file-path checkov-results \
                  --soft-fail || {
                  echo "⚠️ Checkov scan encountered issues, checking for partial results..."
                }
              else
                echo "⚠️ No valid .tfvars files found for variable scanning"
                echo "scan_status=skipped" >> $GITHUB_OUTPUT
                echo "scan_message=⚠️ No valid .tfvars files found" >> $GITHUB_OUTPUT
                echo "failed_checks=0" >> $GITHUB_OUTPUT
                echo "passed_checks=0" >> $GITHUB_OUTPUT
                exit 0
              fi
            else
              echo "ℹ️ No valid .tfvars files in discovered deployments"
              echo "scan_status=skipped" >> $GITHUB_OUTPUT
              echo "scan_message=ℹ️ No valid .tfvars files found" >> $GITHUB_OUTPUT
              echo "failed_checks=0" >> $GITHUB_OUTPUT
              echo "passed_checks=0" >> $GITHUB_OUTPUT
              exit 0
            fi
          else
            echo "ℹ️ No .tfvars files in discovered deployments - skipping security scan"
            echo "scan_status=skipped" >> $GITHUB_OUTPUT
            echo "scan_message=ℹ️ No .tfvars files in deployments" >> $GITHUB_OUTPUT
            echo "failed_checks=0" >> $GITHUB_OUTPUT
            echo "passed_checks=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # Find the actual results file (Checkov creates it with different names)
          echo "🔍 Locating Checkov results file..."
          results_file=""
          
          # Check for main controller scan results
          if [ -f "./checkov-results.json" ]; then
            results_file="./checkov-results.json"
          elif [ -f "./checkov-results/results_json.json" ]; then
            results_file="./checkov-results/results_json.json"
            echo "📁 Found controller results in subdirectory: $results_file"
          elif [ -f "./results_json.json" ]; then
            results_file="./results_json.json"
          fi
          
          # If no controller results, check for source results  
          if [ -z "$results_file" ] || [ ! -f "$results_file" ]; then
            if [ -f "./checkov-results-source/results_json.json" ]; then
              results_file="./checkov-results-source/results_json.json"
              echo "� Found source results: $results_file"
            fi
          fi
          
          # If still no results, try fallback scan
          if [ -z "$results_file" ] || [ ! -f "$results_file" ]; then
            echo "🔄 No scan results found, trying fallback directory scan..."
            checkov --framework terraform \
              --directory ../controller \
              --output json \
              --quiet \
              --soft-fail 2>checkov-error.log 1>./fallback-results.json || {
              echo "❌ All Checkov scan attempts failed"
              echo "📋 Error log:"
              cat checkov-error.log 2>/dev/null || echo "No error log available"
              echo "scan_status=skipped" >> $GITHUB_OUTPUT
              echo "scan_message=⚠️ Checkov scan failed completely" >> $GITHUB_OUTPUT
              echo "failed_checks=0" >> $GITHUB_OUTPUT
              echo "passed_checks=0" >> $GITHUB_OUTPUT
              exit 0
            }
            results_file="./fallback-results.json"
          fi
          
          # Process results
          if [ -n "$results_file" ] && [ -f "$results_file" ]; then
            echo "✅ Checkov results file found: $results_file"
            file_size=$(wc -c < "$results_file")
            echo "📄 Results file size: $file_size bytes"
            
            if [ "$file_size" -lt 10 ]; then
              echo "❌ Results file too small, likely empty"
              echo "scan_status=skipped" >> $GITHUB_OUTPUT
              echo "scan_message=⚠️ Checkov results file empty" >> $GITHUB_OUTPUT
              echo "failed_checks=0" >> $GITHUB_OUTPUT
              echo "passed_checks=0" >> $GITHUB_OUTPUT
              exit 0
            fi
            
            # Debug: Show structure of results
            echo "📋 Results structure:"
            jq keys "$results_file" 2>/dev/null || {
              echo "⚠️ Results file is not valid JSON, trying to parse anyway..."
              cat "$results_file" | head -10
            }
            
            # Extract results with better error handling
            failed_checks=$(jq '.failed' "$results_file" 2>/dev/null || echo "0")
            passed_checks=$(jq '.passed' "$results_file" 2>/dev/null || echo "0")
            resource_count=$(jq '.resource_count' "$results_file" 2>/dev/null || echo "0")
            
            # If using older format, try alternative parsing
            if [ "$failed_checks" = "null" ] || [ "$passed_checks" = "null" ]; then
              echo "🔄 Alternative parsing attempt..."
              failed_checks=$(jq 'if has("results") then .results.failed_checks | length else 0 end' "$results_file" 2>/dev/null || echo "0")
              passed_checks=$(jq 'if has("results") then .results.passed_checks | length else 0 end' "$results_file" 2>/dev/null || echo "0")
              resource_count=$(jq 'if has("summary") then .summary.resource_count else 0 end' "$results_file" 2>/dev/null || echo "0")
            fi
            
            echo "🔍 Security scan completed: $failed_checks failed, $passed_checks passed, $resource_count resources scanned"
            echo "failed_checks=$failed_checks" >> $GITHUB_OUTPUT
            echo "passed_checks=$passed_checks" >> $GITHUB_OUTPUT
            
            # Check if no resources were found to scan
            if [ "$resource_count" = "0" ] || [ "$resource_count" = "null" ]; then
              echo "⚠️ No Terraform resources found to scan"
              echo "scan_status=skipped" >> $GITHUB_OUTPUT
              echo "scan_message=⚠️ No Terraform resources found to scan" >> $GITHUB_OUTPUT
            # Determine status based on findings
            elif [ "$failed_checks" != "0" ] && [ "$failed_checks" -gt 0 ] 2>/dev/null; then
              echo "scan_status=warning" >> $GITHUB_OUTPUT
              echo "scan_message=⚠️ Security issues found ($failed_checks)" >> $GITHUB_OUTPUT
              
              # Extract critical/high severity issues for detailed reporting
              critical_high_issues=$(jq -r '.results.failed_checks[]? | select(.severity == "CRITICAL" or .severity == "HIGH") | "\(.check_id): \(.check_name)"' "$results_file" 2>/dev/null | head -10)
              if [ -n "$critical_high_issues" ]; then
                echo "critical_issues<<EOF" >> $GITHUB_OUTPUT
                echo "$critical_high_issues" >> $GITHUB_OUTPUT
                echo "EOF" >> $GITHUB_OUTPUT
              fi
            else
              echo "scan_status=passed" >> $GITHUB_OUTPUT
              echo "scan_message=✅ Security scan passed ($resource_count resources)" >> $GITHUB_OUTPUT
            fi
            
            # Copy results to standard location for artifact upload
            cp "$results_file" ./checkov-results.json
          else
            echo "❌ Checkov results file not found after scan"
            echo "📁 Debug - listing all files:"
            ls -la . 2>/dev/null
            echo "📁 Debug - listing checkov-* files:"
            ls -la checkov-* 2>/dev/null || echo "No checkov files found"
            echo "📁 Debug - listing *results* files:"
            ls -la *results* 2>/dev/null || echo "No results files found"
            echo "scan_status=skipped" >> $GITHUB_OUTPUT
            echo "scan_message=⚠️ Security scan could not generate results" >> $GITHUB_OUTPUT
            echo "failed_checks=0" >> $GITHUB_OUTPUT
            echo "passed_checks=0" >> $GITHUB_OUTPUT
          fi

      - name: 💬 Post Security Scan Comment
        if: steps.security.outputs.scan_status == 'warning'
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const failed = '${{ steps.security.outputs.failed_checks }}';
            const passed = '${{ steps.security.outputs.passed_checks }}';
            const criticalIssues = `${{ steps.security.outputs.critical_issues }}`;
            
            let comment = `## 🛡️ Security Scan Results\n\n`;
            comment += `⚠️ **Security issues detected!**\n\n`;
            comment += `### 📊 Scan Summary\n`;
            comment += `| Status | Count |\n`;
            comment += `|--------|-------|\n`;
            comment += `| ❌ Failed Checks | ${failed} |\n`;
            comment += `| ✅ Passed Checks | ${passed} |\n\n`;
            
            if (criticalIssues) {
              comment += `### 🔴 Critical/High Severity Issues\n`;
              comment += `\`\`\`\n${criticalIssues}\n\`\`\`\n\n`;
            }
            
            comment += `### ⚠️ Action Required\n`;
            comment += `Security vulnerabilities have been detected in your Terraform configuration. `;
            comment += `Please review and fix these issues before proceeding.\n\n`;
            comment += `**This PR will be closed automatically due to security violations.**\n\n`;
            comment += `---\n`;
            comment += `🤖 *Centralized Controller - Security Scan* | `;
            comment += `[View Run](https://github.com/Terraform-centilazed-pipline/centerlized-pipline-/actions/runs/${{ github.run_id }})`;
            
            await github.rest.issues.createComment({
              owner: '${{ github.event.client_payload.source_owner }}',
              repo: '${{ github.event.client_payload.source_repo }}',
              issue_number: ${{ github.event.client_payload.pr_number }},
              body: comment
            });

      - name: 🛑 Stop if Security Scan Failed
        if: steps.security.outputs.scan_status == 'warning' && steps.security.outputs.failed_checks != '0'
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            // Close the PR
            await github.rest.pulls.update({
              owner: '${{ github.event.client_payload.source_owner }}',
              repo: '${{ github.event.client_payload.source_repo }}',
              pull_number: ${{ github.event.client_payload.pr_number }},
              state: 'closed'
            });
            
            const failed = '${{ steps.security.outputs.failed_checks }}';
            core.setFailed(`❌ Security scan failed with ${failed} violations. PR closed.`);

      

      # ===== STEP 3: TERRAFORM PLAN (Only runs if pre-validation passed) =====
      - name: 📋 Terraform Plan
        if: steps.discover.outputs.has_deployments == 'true'
        id: plan
        working-directory: source-repo
        env:
          TERRAFORM_DIR: ../controller
        run: |
          echo "📋 Running terraform plan for multiple deployments..."
          echo "🔧 Terraform directory: $TERRAFORM_DIR"
          
          # Use the deployment orchestrator to run plans and create artifacts
          # This handles multiple deployments from different accounts properly
          set +e  # Don't exit on error - we want to show results even if some plans fail
          python3 ../controller/scripts/terraform-deployment-orchestrator.py plan \
            --deployments-json deployments.json \
            --output-summary plan-results.json \
            --debug
          
          plan_exit_code=$?
          set -e  # Re-enable exit on error
          
          echo "🔍 Deployment manager exit code: $plan_exit_code"
          
          if [ -f plan-results.json ]; then
            total=$(jq -r '.total_deployments // 0' plan-results.json)
            successful=$(jq -r '.successful_plans // 0' plan-results.json)
            failed=$(jq -r '.failed_plans // 0' plan-results.json)
            has_changes=$(jq -r '.has_changes // false' plan-results.json)
            
            echo "📊 Plan Results: $successful/$total successful, $failed failed"
            
            echo "total_deployments=$total" >> $GITHUB_OUTPUT
            echo "successful_plans=$successful" >> $GITHUB_OUTPUT
            echo "failed_plans=$failed" >> $GITHUB_OUTPUT
            echo "has_changes=$has_changes" >> $GITHUB_OUTPUT
            
            # Select first plan for OPA validation
            if [ -d "terraform-json" ] && [ "$successful" -gt 0 ]; then
              mkdir -p canonical-plan
              FIRST_JSON=$(ls terraform-json/*.json 2>/dev/null | head -n1)
              if [ -n "$FIRST_JSON" ]; then
                cp "$FIRST_JSON" canonical-plan/plan.json
                echo "✅ Selected plan for OPA: $FIRST_JSON"
                echo "opa_plan_ready=true" >> $GITHUB_OUTPUT
              else
                echo "⚠️ No JSON plans found for OPA validation"
                echo "opa_plan_ready=false" >> $GITHUB_OUTPUT
              fi
            else
              echo "⚠️ No successful plans for OPA validation"
              echo "opa_plan_ready=false" >> $GITHUB_OUTPUT
            fi
            
            # Continue workflow even if some plans failed
            if [ "$successful" -gt 0 ]; then
              echo "✅ Continuing workflow with $successful successful plans"
              exit 0
            else
              echo "❌ No plans were successful"
              exit 1
            fi
          else
            echo "❌ No plan-results.json found"
            exit 1
          fi

      # ===== STEP 4: OPA VALIDATION =====
      - name: 🛡️ OPA Validation
        if: steps.plan.outputs.opa_plan_ready == 'true'
        id: opa
        working-directory: source-repo
        run: |
          echo "🛡️ Running OPA validation on canonical plan..."
          echo "📁 Using multi-service policy structure: terraform/main.rego"
          
          if [ ! -f "canonical-plan/plan.json" ]; then
            echo "❌ No canonical plan found"
            exit 1
          fi
          
          # Basic plan analysis
          echo "� Plan analysis:"
          echo "   File size: $(wc -c < canonical-plan/plan.json) bytes"
          echo "   Resource changes: $(jq -r '.resource_changes | length' canonical-plan/plan.json 2>/dev/null || echo "0")"
          
          # Get deployment summary (which services are being deployed)
          echo "📦 Detecting services in plan..."
          opa eval -d ../opa-policies/terraform/ \
            -i canonical-plan/plan.json \
            "data.terraform.main.deployment_summary" \
            --format pretty > opa-summary.txt || true
          
          services_detected=$(opa eval -d ../opa-policies/terraform/ \
            -i canonical-plan/plan.json \
            "data.terraform.main.deployment_summary.services_detected" \
            --format json | jq -r '.result[0].expressions[0].value[]?' 2>/dev/null | tr '\n' ',' | sed 's/,$//')
          
          if [ -n "$services_detected" ]; then
            echo "📦 Services detected: $services_detected"
          else
            echo "ℹ️ No specific services detected, will run all policies"
          fi
          
          # Run unified OPA validation (auto-detects and validates all services)
          echo "🔍 Running policy validation..."
          opa eval -d ../opa-policies/terraform/ \
            -i canonical-plan/plan.json \
            "data.terraform.main.deny" \
            --format pretty > opa-result.txt || true
          
          # Count violations by severity  
          violations_json=$(opa eval -d ../opa-policies/terraform/ \
            -i canonical-plan/plan.json \
            "data.terraform.main.deny" \
            --format json 2>/dev/null || echo '{"result":[{"expressions":[{"value":{}}]}]}')
          
          # Parse violations from OPA object format - violations are stored as keys in the object
          total_violations=$(echo "$violations_json" | jq -r '
            [.result[0].expressions[0].value // {} | 
             if type == "object" then keys[] else empty end |
             try fromjson catch empty |
             select(type == "object")] | length' 2>/dev/null || echo "0")
             
          critical_violations=$(echo "$violations_json" | jq -r '
            [.result[0].expressions[0].value // {} | 
             if type == "object" then keys[] else empty end |
             try fromjson catch empty |
             select(type == "object" and .severity == "critical")] | length' 2>/dev/null || echo "0")
             
          high_violations=$(echo "$violations_json" | jq -r '
            [.result[0].expressions[0].value // {} | 
             if type == "object" then keys[] else empty end |
             try fromjson catch empty |
             select(type == "object" and .severity == "high")] | length' 2>/dev/null || echo "0")
             
          medium_violations=$(echo "$violations_json" | jq -r '
            [.result[0].expressions[0].value // {} | 
             if type == "object" then keys[] else empty end |
             try fromjson catch empty |
             select(type == "object" and .severity == "medium")] | length' 2>/dev/null || echo "0")
             
          low_violations=$(echo "$violations_json" | jq -r '
            [.result[0].expressions[0].value // {} | 
             if type == "object" then keys[] else empty end |
             try fromjson catch empty |
             select(type == "object" and .severity == "low")] | length' 2>/dev/null || echo "0")
          
          # Ensure variables are numeric (default to 0 if empty, remove any whitespace/newlines)
          total_violations=$(echo "${total_violations:-0}" | tr -d '\n\r' | grep -o '[0-9]*' || echo "0")
          critical_violations=$(echo "${critical_violations:-0}" | tr -d '\n\r' | grep -o '[0-9]*' || echo "0")
          high_violations=$(echo "${high_violations:-0}" | tr -d '\n\r' | grep -o '[0-9]*' || echo "0")
          medium_violations=$(echo "${medium_violations:-0}" | tr -d '\n\r' | grep -o '[0-9]*' || echo "0")
          low_violations=$(echo "${low_violations:-0}" | tr -d '\n\r' | grep -o '[0-9]*' || echo "0")
          
          echo ""
          echo "📊 Validation Results:"
          echo "   Total violations: $total_violations"
          echo "   🔴 Critical: $critical_violations"
          echo "   🟠 High: $high_violations"
          echo "   🟡 Medium: $medium_violations"
          echo "   🟢 Low: $low_violations"
          
          # Set outputs
          echo "total_violations=$total_violations" >> $GITHUB_OUTPUT
          echo "critical_violations=$critical_violations" >> $GITHUB_OUTPUT
          echo "high_violations=$high_violations" >> $GITHUB_OUTPUT
          echo "medium_violations=$medium_violations" >> $GITHUB_OUTPUT
          echo "low_violations=$low_violations" >> $GITHUB_OUTPUT
          echo "services_detected=$services_detected" >> $GITHUB_OUTPUT
          
          if [ "$total_violations" -eq 0 ]; then
            echo "validation_status=passed" >> $GITHUB_OUTPUT
            echo ""
            echo "✅ All policies passed - deployment authorized!"
          else
            echo "validation_status=failed" >> $GITHUB_OUTPUT
            echo ""
            echo "❌ Validation failed: $total_violations violations found"
            echo ""
            echo "📋 Violation Details:"
            cat opa-result.txt
          fi

      # ===== STEP 4: POST PR COMMENT =====
      - name: 💬 Post PR Comment
        if: always() && steps.discover.outputs.has_deployments == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const fs = require('fs');
            
            let comment = `## 🚀 Centralized Terraform Controller Results\n\n`;
            comment += `**Source Repo**: \`${{ github.event.client_payload.source_repo }}\`\n`;
            comment += `**PR**: [#${{ github.event.client_payload.pr_number }}](${{ github.event.client_payload.pr_url }})\n`;
            comment += `**Branch**: \`${{ github.event.client_payload.pr_head_ref }}\`\n\n`;
            
            // Plan results
            const total = '${{ steps.plan.outputs.total_deployments }}';
            const successful = '${{ steps.plan.outputs.successful_plans }}';
            const failed = '${{ steps.plan.outputs.failed_plans }}';
            const hasChanges = '${{ steps.plan.outputs.has_changes }}';
            
            if (total && total !== '0') {
              comment += `### 📊 Terraform Plan Summary\n`;
              comment += `| Metric | Count |\n`;
              comment += `|--------|-------|\n`;
              comment += `| 📋 Total Deployments | ${total} |\n`;
              comment += `| ✅ Successful Plans | ${successful} |\n`;
              comment += `| ❌ Failed Plans | ${failed} |\n`;
              comment += `| 🔄 Has Changes | ${hasChanges === 'true' ? 'Yes' : 'No'} |\n\n`;
              
              // Add detailed plan results if available
              try {
                const planResults = fs.readFileSync('source-repo/plan-results.json', 'utf8');
                const results = JSON.parse(planResults);
                
                if (results.plans && results.plans.length > 0) {
                  comment += `### 📋 Deployment Details\n`;
                  comment += `| Deployment | Status | Changes |\n`;
                  comment += `|------------|--------|----------|\n`;
                  
                  results.plans.forEach(plan => {
                    const status = plan.status === 'success' ? '✅' : '❌';
                    const changes = plan.has_changes ? '🔄 Yes' : '➖ None';
                    comment += `| ${plan.deployment} | ${status} | ${changes} |\n`;
                  });
                  comment += `\n`;
                }
                
                // Add plan markdown if available
                const planMarkdownDir = 'source-repo/plan-markdown';
                if (fs.existsSync(planMarkdownDir)) {
                  const markdownFiles = fs.readdirSync(planMarkdownDir).filter(f => f.endsWith('.md'));
                  
                  if (markdownFiles.length > 0) {
                    comment += `### 🔧 Terraform Plans\n`;
                    markdownFiles.forEach(file => {
                      const content = fs.readFileSync(`${planMarkdownDir}/${file}`, 'utf8');
                      // Truncate if too long
                      const truncated = content.length > 5000 ? content.substring(0, 5000) + '\n... (truncated)' : content;
                      comment += truncated + '\n\n';
                    });
                  }
                }
              } catch (e) {
                console.log('Could not read plan results:', e.message);
              }
            } else {
              comment += `### 📋 Terraform Plan\n`;
              comment += `ℹ️ No deployments found or no changes detected\n\n`;
            }
            
            // OPA results
            const validation = '${{ steps.opa.outputs.validation_status }}';
            const violations = '${{ steps.opa.outputs.total_violations }}';
            
            comment += `### 🛡️ OPA Policy Validation\n`;
            if (validation === 'passed') {
              comment += `✅ **Status**: PASSED - No policy violations\n\n`;
            } else if (validation === 'failed') {
              comment += `❌ **Status**: FAILED\n`;
              comment += `- **Violations**: ${violations}\n\n`;
              try {
                const opaResult = fs.readFileSync('source-repo/opa-result.txt', 'utf8');
                comment += `\`\`\`\n${opaResult}\n\`\`\`\n\n`;
              } catch (e) {}
            } else if ('${{ steps.plan.outputs.opa_plan_ready }}' === 'false') {
              comment += `⚠️ **Status**: SKIPPED - No successful plans for validation\n\n`;
            }
            
            // Security scan results
            const securityStatus = '${{ steps.security.outputs.scan_status }}';
            const failedChecks = '${{ steps.security.outputs.failed_checks }}';
            const passedChecks = '${{ steps.security.outputs.passed_checks }}';
            
            comment += `### 🛡️ Security Scan (Checkov)\n`;
            if (securityStatus === 'passed') {
              comment += `✅ **Status**: PASSED - No security issues\n`;
              comment += `- **Checks**: ${passedChecks} passed\n\n`;
            } else if (securityStatus === 'warning') {
              comment += `⚠️ **Status**: ISSUES FOUND\n`;
              comment += `- **Failed Checks**: ${failedChecks}\n`;
              comment += `- **Passed Checks**: ${passedChecks}\n\n`;
            } else if (securityStatus === 'skipped') {
              comment += `⚠️ **Status**: SKIPPED - Could not run security scan\n\n`;
            } else {
              comment += `ℹ️ **Status**: Security scan not run\n\n`;
            }
            
            comment += `\n---\n`;
            comment += `🤖 *Centralized Controller* | `;
            comment += `[View Run](https://github.com/Terraform-centilazed-pipline/centerlized-pipline-/actions/runs/${{ github.run_id }})`;
            
            await github.rest.issues.createComment({
              owner: '${{ github.event.client_payload.source_owner }}',
              repo: '${{ github.event.client_payload.source_repo }}',
              issue_number: ${{ github.event.client_payload.pr_number }},
              body: comment
            });

      # ===== STEP 5: AUTO-MERGE OR CLOSE PR =====
      - name: 🔀 Handle PR - Auto-merge if OPA Passed
        if: steps.opa.outputs.validation_status != ''
        id: merge
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const validation = '${{ steps.opa.outputs.validation_status }}';
            const owner = '${{ github.event.client_payload.source_owner }}';
            const repo = '${{ github.event.client_payload.source_repo }}';
            const pr_number = ${{ github.event.client_payload.pr_number }};
            
            if (validation === 'passed') {
              console.log('✅ OPA passed - auto-merging PR');
              
              try {
                const { data: mergeResult } = await github.rest.pulls.merge({
                  owner,
                  repo,
                  pull_number: pr_number,
                  merge_method: 'squash',
                  commit_title: '[Terraform] Auto-approved: OPA validation passed',
                  commit_message: 'Automatically merged after successful OPA policy validation.\n\nAll security policies passed. Terraform changes approved for deployment.'
                });
                
                console.log('✅ PR merged successfully');
                console.log('Merge SHA:', mergeResult.sha);
                
                // Set output for triggering apply job
                core.setOutput('merged', 'true');
                core.setOutput('merge_sha', mergeResult.sha);
                
                // Add success comment
                await github.rest.issues.createComment({
                  owner,
                  repo,
                  issue_number: pr_number,
                  body: `✅ **PR Auto-Approved & Merged!**\n\n` +
                        `🛡️ OPA validation passed - all security policies compliant\n` +
                        `� Services validated: ${{ steps.opa.outputs.services_detected || 'All services' }}\n` +
                        `�🔀 Changes have been merged to \`main\`\n` +
                        `🚀 Terraform apply will begin automatically...\n\n` +
                        `**Merge SHA**: \`${mergeResult.sha}\``
                });
                
              } catch (error) {
                console.log('❌ Could not merge:', error.message);
                core.setOutput('merged', 'false');
                
                await github.rest.issues.createComment({
                  owner,
                  repo,
                  issue_number: pr_number,
                  body: `⚠️ **Auto-merge Failed**\n\nOPA validation passed, but automatic merge failed.\n\nError: ${error.message}\n\nPlease merge manually.`
                });
              }
            } else if (validation === 'failed') {
              console.log('❌ OPA failed - closing PR');
              
              core.setOutput('merged', 'false');
              
              await github.rest.pulls.update({
                owner,
                repo,
                pull_number: pr_number,
                state: 'closed'
              });
              
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: pr_number,
                body: `❌ **PR Closed: Policy Violations**\n\n` +
                      `🛡️ OPA validation failed with **${{ steps.opa.outputs.total_violations }} violations**\n\n` +
                      `**Severity Breakdown**:\n` +
                      `- 🔴 Critical: ${{ steps.opa.outputs.critical_violations }}\n` +
                      `- 🟠 High: ${{ steps.opa.outputs.high_violations }}\n` +
                      `- 🟡 Medium: ${{ steps.opa.outputs.medium_violations }}\n` +
                      `- 🟢 Low: ${{ steps.opa.outputs.low_violations }}\n\n` +
                      `**Services Validated**: ${{ steps.opa.outputs.services_detected || 'All' }}\n\n` +
                      `This PR has been automatically closed due to security policy violations.\n\n` +
                      `**Required Actions**:\n` +
                      `1. Review the OPA validation results above\n` +
                      `2. Fix all policy violations in your configuration\n` +
                      `3. Create a new PR with corrected changes\n\n` +
                      `---\n` +
                      `*Security policies must pass before changes can be merged.*`
              });
              
              console.log('❌ PR closed due to policy violations');
            }

      # ===== STEP 6: TRIGGER TERRAFORM APPLY (after successful merge) =====
      - name: 🚀 Trigger Terraform Apply
        if: steps.merge.outputs.merged == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            console.log('🚀 Triggering Terraform Apply workflow...');
            
            // Dispatch apply event to this same workflow
            await github.rest.repos.createDispatchEvent({
              owner: 'Terraform-centilazed-pipline',
              repo: 'centerlized-pipline-',
              event_type: 'terraform_apply',
              client_payload: {
                source_owner: '${{ github.event.client_payload.source_owner }}',
                source_repo: '${{ github.event.client_payload.source_repo }}',
                pr_number: ${{ github.event.client_payload.pr_number }},
                pr_url: '${{ github.event.client_payload.pr_url }}',
                merge_commit_sha: '${{ steps.merge.outputs.merge_sha }}',
                action: 'apply',
                merged: 'true'
              }
            });
            
            console.log('✅ Apply workflow triggered');

      # ===== TERRAFORM APPLY (when action == 'apply') =====
      - name: 🚀 Terraform Apply
        if: github.event.client_payload.action == 'apply' && (steps.discover.outputs.has_deployments == 'true' || steps.discover-apply.outputs.has_deployments == 'true')
        id: apply
        working-directory: source-repo
        env:
          TERRAFORM_DIR: ../controller
        run: |
          echo "🚀 Running terraform apply for deployments..."
          echo "🔧 Terraform directory: $TERRAFORM_DIR"
          
          # Use the deployment orchestrator to run applies
          set +e  # Don't exit on error - we want to show results even if some applies fail
          python3 ../controller/scripts/terraform-deployment-orchestrator.py apply \
            --deployments-json deployments.json \
            --output-summary apply-results.json \
            --debug
          
          apply_exit_code=$?
          set -e
          
          echo "🔍 Apply exit code: $apply_exit_code"
          
          if [ -f apply-results.json ]; then
            total=$(jq -r '.total_deployments // 0' apply-results.json)
            successful=$(jq -r '.successful_applies // 0' apply-results.json)
            failed=$(jq -r '.failed_applies // 0' apply-results.json)
            
            echo "📊 Apply Results: $successful/$total successful, $failed failed"
            
            echo "total_deployments=$total" >> $GITHUB_OUTPUT
            echo "successful_applies=$successful" >> $GITHUB_OUTPUT
            echo "failed_applies=$failed" >> $GITHUB_OUTPUT
            
            if [ "$successful" -gt 0 ]; then
              echo "✅ Applied $successful deployments successfully"
              if [ "$failed" -gt 0 ]; then
                echo "⚠️ $failed deployments failed"
                # Don't exit 1 here - let the PR comment show the results
              fi
            else
              echo "❌ No successful applies"
              # Don't exit 1 here - let the PR comment show the results
            fi
          else
            echo "❌ No apply-results.json found"
          fi

      - name: 💬 Post Apply Results to PR
        if: github.event.client_payload.action == 'apply' && always() && (steps.discover.outputs.has_deployments == 'true' || steps.discover-apply.outputs.has_deployments == 'true')
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const fs = require('fs');
            
            let comment = `## 🚀 Terraform Apply Results\n\n`;
            comment += `**Merged PR**: [#${{ github.event.client_payload.pr_number }}](https://github.com/${{ github.event.client_payload.source_owner }}/${{ github.event.client_payload.source_repo }}/pull/${{ github.event.client_payload.pr_number }})\n`;
            comment += `**Applied to**: \`main\` branch\n\n`;
            
            const total = '${{ steps.apply.outputs.total_deployments }}';
            const successful = '${{ steps.apply.outputs.successful_applies }}';
            const failed = '${{ steps.apply.outputs.failed_applies }}';
            
            if (total && total !== '0') {
              comment += `### 📊 Apply Summary\n`;
              comment += `| Metric | Count |\n`;
              comment += `|--------|-------|\n`;
              comment += `| 📋 Total Deployments | ${total} |\n`;
              comment += `| ✅ Successful Applies | ${successful} |\n`;
              comment += `| ❌ Failed Applies | ${failed} |\n\n`;
              
              // Add detailed results if available
              try {
                const applyResults = fs.readFileSync('source-repo/apply-results.json', 'utf8');
                const results = JSON.parse(applyResults);
                
                if (results.applies && results.applies.length > 0) {
                  comment += `### 📋 Deployment Details\n`;
                  comment += `| Deployment | Status | Message |\n`;
                  comment += `|------------|--------|----------|\n`;
                  
                  results.applies.forEach(apply => {
                    const status = apply.status === 'success' ? '✅' : '❌';
                    const message = apply.message || 'N/A';
                    comment += `| ${apply.deployment} | ${status} | ${message.substring(0, 50)} |\n`;
                  });
                  comment += `\n`;
                }
              } catch (e) {
                console.log('Could not read apply results:', e.message);
              }
              
              if (failed === '0') {
                comment += `\n✅ **All deployments applied successfully!**\n`;
              } else {
                comment += `\n⚠️ **Some deployments failed - check logs for details**\n`;
              }
            } else {
              comment += `ℹ️ No deployments were applied\n`;
            }
            
            comment += `\n---\n`;
            comment += `🤖 *Centralized Controller - Apply* | `;
            comment += `[View Run](https://github.com/Terraform-centilazed-pipline/centerlized-pipline-/actions/runs/${{ github.run_id }})`;
            
            await github.rest.issues.createComment({
              owner: '${{ github.event.client_payload.source_owner }}',
              repo: '${{ github.event.client_payload.source_repo }}',
              issue_number: ${{ github.event.client_payload.pr_number }},
              body: comment
            });

      # ===== UPLOAD ARTIFACTS =====
      - name: 📦 Upload Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: terraform-controller-results
          path: |
            source-repo/deployments.json
            source-repo/plan-results.json
            source-repo/apply-results.json
            source-repo/terraform-json/
            source-repo/plan-markdown/
            source-repo/canonical-plan/
            source-repo/plans/
            source-repo/logs/
            source-repo/opa-result.txt
            source-repo/checkov-results.json
            source-repo/checkov-*.json
            source-repo/*.log
